listen: 127.0.0.1:9876  # (default: disabled)
# timezone: Asia/Shanghai  # Timezone for status_schedule (default: system local)

services:
  - name: service1
    interval: 30s
    failure_threshold: 3  # (default: 3)
    success_threshold: 1  # (default: 1)
    # log:
    #   file: /var/log/mon/service1.log  # (default: stderr)
    #   max_size: 100  # MB (default: 100)
    #   max_backups: 5  # (default: 5)
    #   max_age: 30  # days (default: 30)
    #   compress: false  # (default: false)
    checker:
      # type: custom
      # custom:
      #   command: '/usr/local/bin/check.sh'  # Should return 0 for success, non-zero for failure
      #   shell: bash  # (default: bash)
      #   timeout: 10s
      type: http_get
      http_get:
        url: https://api.example.com/health
        timeout: 10s
        # follow_redirects: false  # (default: false)
        # proxy_url: socks5h://127.0.0.1:1080  # Supports http/https/socks5/socks5h (default: disabled)
      # type: ping
      # ping:
      #   host: 192.168.1.1
      #   timeout: 10s
    notifiers:
      # - name: custom1
      #   type: custom
      #   custom:
      #     command: '/usr/local/bin/notify.sh'  # MON_SUBJECT and MON_BODY environment variables will be set
      #     shell: bash  # (default: bash)
      #     timeout: 10s
      #     max_attempts: 3  # (default: 3)
      #     retry_initial_delay: 1s  # (default: 1s)
      #     retry_max_delay: 30s  # (default: 30s)
      #   status_schedule: '0 9 * * *'  # (default: disabled)
      # - name: discord1
      #   type: discord
      #   discord:
      #     webhook_url: 'YOUR_WEBHOOK_URL'
      #     timeout: 10s
      #     max_attempts: 3  # (default: 3)
      #     retry_initial_delay: 1s  # (default: 1s)
      #     retry_max_delay: 30s  # (default: 30s)
      #     proxy_url: socks5h://127.0.0.1:1080  # Supports http/https/socks5/socks5h (default: disabled)
      #   status_schedule: '0 9 * * *'  # (default: disabled)
      # - name: feishu1
      #   type: feishu
      #   feishu:
      #     webhook_url: 'YOUR_WEBHOOK_URL'
      #     secret: 'YOUR_SECRET'  # (default: disabled)
      #     timeout: 10s
      #     max_attempts: 3  # (default: 3)
      #     retry_initial_delay: 1s  # (default: 1s)
      #     retry_max_delay: 30s  # (default: 30s)
      #     proxy_url: socks5h://127.0.0.1:1080  # Supports http/https/socks5/socks5h (default: disabled)
      #   status_schedule: '0 9 * * *'  # (default: disabled)
      # - name: smtp1
      #   type: smtp
      #   smtp:
      #     host: smtp.example.com
      #     port: 465
      #     username: alice@example.com
      #     password: 'YOUR_PASSWORD'
      #     from: alice@example.com
      #     to:
      #       - bob@example.com
      #     tls_mode: tls  # none | starttls | tls  # (default: none)
      #     tls_skip_verify: false  # (default: false)
      #     timeout: 10s
      #     max_attempts: 3  # (default: 3)
      #     retry_initial_delay: 1s  # (default: 1s)
      #     retry_max_delay: 30s  # (default: 30s)
      #     proxy_url: socks5h://127.0.0.1:1080  # SMTP only supports socks5/socks5h (default: disabled)
      #   status_schedule: '0 9 * * *'  # (default: disabled)
      # - name: spug1
      #   type: spug
      #   spug:
      #     user_id: 'YOUR_USER_ID'
      #     channel: 'voice|sms|mail'  # Supported values: voice, sms, mail, wx_mp, wx, dd, fs (default: '')
      #     timeout: 10s
      #     max_attempts: 3  # (default: 3)
      #     retry_initial_delay: 1s  # (default: 1s)
      #     retry_max_delay: 30s  # (default: 30s)
      #     proxy_url: socks5h://127.0.0.1:1080  # Supports http/https/socks5/socks5h (default: disabled)
      #   status_schedule: '0 9 * * *'  # (default: disabled)
      - name: telegram1
        type: telegram
        telegram:
          bot_token: 'YOUR_BOT_TOKEN'
          chat_id: 'YOUR_CHAT_ID'
          timeout: 10s
          # max_attempts: 3  # (default: 3)
          # retry_initial_delay: 1s  # (default: 1s)
          # retry_max_delay: 30s  # (default: 30s)
          # base_url: https://api.telegram.org  # (default: https://api.telegram.org)
          # proxy_url: socks5h://127.0.0.1:1080  # Supports http/https/socks5/socks5h (default: disabled)
        # status_schedule: '0 9 * * *'  # (default: disabled)

# bots:
#   - name: telegram1
#     type: telegram
#     telegram:
#       bot_token: 'YOUR_BOT_TOKEN'
#       allowed_chats:
#         - 'YOUR_CHAT_ID'
#       base_url: https://api.telegram.org  # (default: https://api.telegram.org)
#       proxy_url: socks5h://127.0.0.1:1080  # Supports http/https/socks5/socks5h (default: disabled)
